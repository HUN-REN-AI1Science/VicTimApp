[project]
name = "formind"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["numpy>=2.0", "ecocore"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/formind"]

[tool.uv.sources]
ecocore = { workspace = true }
